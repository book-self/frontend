import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
    review: {
        width: '100%',
        maxWidth: '750px',
        margin: '3rem 0',
        position: 'relative'
    },

    textField: {
        padding: "30px",
    },

    iconButton: {
      position: 'absolute',
      top: '-3.5rem',
      right: '15px'
    }
});
